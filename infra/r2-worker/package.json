{
  "name": "gangroll-r2-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that mints presigned R2 upload URLs after verifying a Firebase ID token.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
